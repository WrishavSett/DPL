# --- Core stack (pinned per project spec) ---
ultralytics==8.4.48
supervision==0.28.0
fastapi==0.136.3
uvicorn[standard]==0.49.0

# opencv-python-headless instead of opencv-python:
# workers run with no display (cv2.imshow removed), headless build avoids
# pulling in GUI/X11 dependencies on a server.
opencv-python-headless==4.13.0.92

# --- Supporting libraries ---
numpy>=1.26,<2.0
pydantic>=2.7,<3.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9
websockets>=12.0
aiofiles>=23.2
apscheduler>=3.10,<4.0
python-dotenv>=1.0

# --- Testing ---
pytest>=8.0